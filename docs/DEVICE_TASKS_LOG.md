# DEVICE_TASKS_LOG.md — execution log for docs/DEVICE_TASKS.md

One line per task: status (done / built-unverified / blocked), commit hash, and any
`DEVICE_PLAN.md` §10 UNVERIFIED item the task settled (with the answer).

| Task | Status | Commit | Notes |
|------|--------|--------|-------|
| D0.1 | done | 90f7b7b | PROTOCOL.md §8 items 0–14 applied; §14 Device authentication added. |
| D0.2 | done | ec2212e | SERVER_PLAN.md: deviceSecrets/setupCodes/contactRequests, devices fields, S2.2/S2b.1/S4.1/S4.2 endpoints. |
| S1.1 | done | a5dbd34 | deviceSecrets store + 64-wide replay window transactions; mqttPasswordHash migration off devices.py deferred to S2.2 per task's own Files list (correct per dependency graph). Window arithmetic: implemented full 64-wide accept (0 < upN−n ≤ 64) reconciling §2.5's stated "64 wide" with its literal inequality. |
| F3.1 | done | 721b266 | partitions.csv (nvs/otadata/assets/ota_0/ota_1) on 16 MB flash per DEVICE_PLAN.md's Walter hardware note; ident.c/h; main.c wired for nvs_flash_init()+ident_load() (touched though not in Files list — required by the task's own Do/Verify text, flagged). idf.py build green via espressif/idf:release-v5.2 Docker image. Boot-log format built-unverified: no hardware attached to check the actual serial line. IDENT_FLAG_REQ_SIG bit position inferred (not specified in plan) from bundle example. |
| S1.2 | done | 8298485 | devauth.py + wirecbor.py + 8 tools/authvectors.json vectors. **Flag for human review:** PROTOCOL.md §10 never assigns a numeric CBOR key to the `lock` field nested inside `cfg` (only `cfg`'s own inner fields `clear=0,auto=1` are keyed); agent filled the gap with `CFG_KEYMAP={"lock":0}`, documented in wirecbor.py's docstring, pending an explicit PROTOCOL.md allocation. |
| F3.2 | done | dae9fc5 | cbor.c/h encoder/decoder, no malloc, rejects indefinite lengths; firmware/host/test_cbor.c round-trips all 8 authvectors byte-identically. Added `cbor_w_map_key` beyond literal spec text for keyed nested maps (cfg/lock), documented in cbor.h. `make test` and `idf.py build` both green. |
| S1.3 | done | f5f608d | ingest verify-before-parse pipeline, authMode/wire fields, replay window, unsigned-LWT exception, sigFailures 10-min rolling window on devices/{d} (device_secrets.py's own counter is lifetime, left untouched). **UNVERIFIED settled:** PROTOCOL.md §14.6 parenthetical says unsigned LWT must omit `session`, but §5.2's own LWT example and DEVICE_TASKS.md's S1.3 brief include it — implemented per §5.2/task brief (session present), flagged in wire.py docstring; PROTOCOL.md §14.6 wording should be fixed. **TODO(orchestrator):** clear_auth_alarm/invalidate_secret_cache exist but nothing calls them yet — wire them up when a rotation endpoint (S2.2) lands. |
| F3.3 | done | 6305872 | auth.c: CBOR-only sign/verify (devices never speak JSON on the wire), key supplied via new `auth_init(kdev)` rather than as a per-call arg (keeps host-testable, no ident.c dep). auth_verify trims sig bytes without rewriting the map header pair count per DEVICE_PLAN §2.4's no-re-serialisation rule. 32-wide down window confirmed intentional vs S1.1's 64-wide up window (asymmetric by design). Host HMAC/SHA-256 fallback for non-ESP32 builds, verified against known test vectors and authvectors.json. make test + idf.py build both green. |
| S1.4 | done | 5693d82 | broker.publish_down(device_id, obj): single choke point, reads wire/authMode, signs or leaves unsigned per authMode, fails closed if device/secrets missing. pager.py backend rewired through it. **TODO(orchestrator):** ingest.py's `_send_system_reply` (unknown-recipient auto-reply) still publishes /down directly, unsigned, outside this choke point — not in S1.4's Files list, needs a follow-up task. Also: POST /api/admin/devices creates authMode:"hmac" devices with no deviceSecrets row yet (that's S2.2's job) — such devices can't receive /down until S2.2 lands; this is expected mid-sequence, not a bug. |
| F3.4 | done | 4b63d4f | net.cpp reads ident_get_*() for credentials; CA-to-slot-12 write now skipped when ca_hash unchanged; net_tls_profile_bootstrap() (profile 3, no cert validation); net_get_rssi/net_publish_raw added. **UNVERIFIED settled:** DEVICE_PLAN.md §5.4's "getRSSI() vs getSignalQuality()" resolved to `WalterModem::getRSSI()` (AT+CSQ, WalterModem.cpp:4478) — getSignalQuality() returns RSRP/RSRQ, a different quantity; raw value 99 ("not known") guarded to return false. **UNVERIFIED settled:** publish binary-safety confirmed — WalterModem::mqttPublish() (WalterMQTT.cpp:97) sends payload as raw bytes via uart_write_bytes, no JSON-string fallback needed; net_publish_raw() implemented directly. idf.py build green. |
| T1.5 | done, with a Blocked item below | 16379f5 | pager_client.py signs/verifies both encodings, 32-wide down window; e2e_v2.py provisions deviceSecrets rows directly (test-harness plumbing, S2.2 not landed yet). Also fixed (flagged, out-of-scope but real bug): ingest.py's `_send_system_reply` was publishing unsigned /down outside S1.4's `publish_down` choke point — now routed through it. `--wire json` e2e: ALL PASSED (9 scenarios). `--wire cbor` e2e: **fails end-to-end**, see Blocked section. Default wire for pager_client.py set to `"json"` (DEVICE_PLAN.md §2.4 wins over DEVICE_TASKS.md's stated "default cbor" per the docs' own precedence rule). |

## Blocked (resolved items kept for history — see "RESOLVED" markers)

- **`BROKER_MANAGES_AUTH=0` shared-`boot`-user bootstrap mode is unimplemented.**
  `DEVICE_PLAN.md` §3.2 itself marks this "NEEDS HUMAN DECISION" and never specifies how a single
  shared MQTT credential (one `boot` user for every in-flight setup code, instead of per-code
  `boot-{bid}`) gets topic-level isolation — a wildcard `pager/boot/+/{down,up}` ACL on one shared
  identity means any bootstrapping device could read/write any other's retained bundle unless
  something else scopes it. `devsetup.py`/`emqx_admin.py`/`pager_client.py` all only implement the
  per-`bid` credential path today; `BROKER_MANAGES_AUTH=0` just makes broker-push calls no-ops
  ("manual" mode), it doesn't switch to a shared identity. **Decision needed:** either specify the
  isolation mechanism for a shared boot user (e.g. a per-code secret embedded in the topic path or
  payload itself) and implement it, or drop this mode from the plan if manual/per-device bootstrap
  is judged sufficient for the no-managed-broker case.


- **RESOLVED by commit `40532fe` (server-architect escalation).** CBOR wire mode cannot round-trip through the real EMQX broker. `tools/emqx_setup.py`'s rule
  SQL (`upsert_action` `"body": "${.}"` at line 134, `upsert_rule` `sql = "SELECT topic, payload,
  qos, clientid FROM ..."` at line 170) makes EMQX serialize the whole rule-engine event —
  including the raw MQTT `payload` — as one JSON string for the webhook POST. EMQX's JSON encoder
  requires valid UTF-8, so a binary CBOR payload gets its invalid bytes replaced with U+FFFD before
  `relay/app/broker.py`'s `parse_webhook` (~line 191-199, which does `payload.encode("utf-8")` on
  the JSON string) ever sees the real bytes. `relay/app/broker.py`'s own module docstring already
  half-acknowledges this gap. Confirmed by T1.5's agent: CBOR+HMAC application logic itself is
  correct (proven via direct `Ingest.handle_up` calls in `test_devauth.py`/`test_ingest.py`); the
  break is purely in the EMQX-rule → JSON-webhook hop.
  **Decision needed:** how to make the EMQX webhook path binary-safe — e.g. base64/hex-encode
  `payload` in the rule SQL and decode it back in `parse_webhook`. This likely belongs in S2.1
  (`tools/emqx_setup.py` is already in that task's Files list) or a small new task, since neither
  currently owns `relay/app/broker.py`'s webhook-parsing side of the fix. Blocks the plan's
  Definition-of-done requirement that `e2e_v2.py` run green "in both wire encodings."
  **UPDATE (S4.5):** broader than first thought — `relay/app/broker.py`'s `publish()` choked on
  any binary payload, not just CBOR. **Fix (`40532fe`):** EMQX's REST publish API takes
  `payload_encoding: "base64"`; the webhook rule SQL now adds `base64_encode(payload) as
  payload_b64` alongside the existing plain `payload` column (kept for operator readability /
  back-compat). Confirmed empirically against a live EMQX 5.8.0 (sniffed the actual webhook body).
  20 new tests (`test_broker.py`) pin the exact failing byte from the original traceback.
  `tools/e2e_v2.py address_book` now passes in **both** `--wire json` and `--wire cbor` against
  the real stack. `docs/SERVER_PLAN.md` §9.4's hand-configured production rule SQL was also fixed
  (it still said plain `payload`, which would have silently broken CBOR devices in prod).

- **RESOLVED by commit `f14c904`.** `tools/e2e_v2.py` was stale against S2.2's response shape
  (`create_device_with_secret` read `info["mqttPassword"]`, but S2.2 returns
  `{device, setupCode, expiresAt, brokerPush, manualAcl}` with no plaintext secret). **Fix:**
  `create_device_with_secret` now calls the real admin create-device endpoint, then
  `pager_client.bootstrap_device(setupCode, ...)` — T2b.4/S2b.5's already-verified real bootstrap
  path — to get a working `DeviceClient`, instead of reading a plaintext password or writing
  directly into `deviceSecrets`. No new security surface added (no test-only secret-exposing
  endpoint needed). Also fixed the `scenario_fanout` leftover-armed-failure leak into later
  scenarios (drains via `POST /_reset`). **Full default scenario suite (11 scenarios, including
  `address_book` and `setup_code`) now passes in both `--wire json` and `--wire cbor`** against the
  real stack — this clears the plan's Definition-of-done e2e requirement.

- **RESOLVED by commit `f14c904`** (same fix as above). `scenario_fanout`'s leftover armed
  failures no longer leak into later scenarios.

| S2.1 | done | f6cf19b | emqx_admin.py (ensure_device/ensure_boot_user/delete_user), BROKER_MANAGES_AUTH=0 no-op mode, emqx.conf (built_in_database authn/authz, deny-by-default), tools/emqx_setup.py provisions the relay-1 credential. 10/10 tests pass. Hand-verified against a live container: unknown user refused (CONNACK reason 5); pgr-0001 cannot publish to pgr-0002's topic (broker log: not_authorized, subscriber never received it) but can publish to its own. Noted PROTOCOL.md §2's "three ACL rules" is imprecise — actual mirror-image enforcement needs 4 rules (3 publish + 1 subscribe) per device; implemented the working version, flagged the doc wording. |
| S4.1 | done | 7de2767 | contacts.py store, ingest contact_req handling, admin approve/reject/backends endpoints, firestore.rules. Dedup, 5-pending cap, book-version bump all in place. push_book(device_id) left as a logging no-op stub in contacts.py per orchestrator note — S4.2 replaces its body. **Flagged, not resolved:** PROTOCOL.md §3.1 says `ph` covers "phone or alias reference" but §10's keymap has no separate alias wire key — implemented `ph` starting with `+` as E.164, else treated as alias. `devices.bookVersion`/`backends.adminVerified` written as raw undeclared fields (devices.py/backends.py models out of scope) — will silently round-trip via extra="ignore" until a task with those files in scope declares them properly. |
| F3.5 | done | 1dda3a2 | setup.c: parse/derive/decrypt/decode/validate/ident_store/CA-write/ack, esp_console `setup <code>` command replacing main.c's old halt loop. Used tools/setup_code_vectors.json (S2b.1's actual vector location, not authvectors.json). Host test links real mbedtls (not a hand-rolled fallback like auth.c's, since hand-writing AES-GCM was judged too risky) via pkg-config/Homebrew. **Touched beyond Files list, flagged:** net.cpp/net.h gained net_bootstrap_attach/connect/write_ca and a topic-aware RX size cap (4kB for pager/boot/*, 640B elsewhere) — judged required by the task's own explicit bootstrap-hop requirements (profile 3, no CA, boot-{bid} creds), not a design guess. make test + idf.py build both green; `setup <code>` reaching SETUP done is built-unverified: no hardware attached. |
| F8.1 | done | 0a23897 | firmware/README.md measurement tables updated with real values from this run: M9 (Noto assets, 583 KiB, CJK-tofu caveat), M13 (arrow codes/esc/tab, input_decode_key() cite), new M16-M20 (I2C-across-sleep, UI-awake current, RSSI bucket mapping, battery thresholds, CardKB usability) and M21-M23 (on-device PBKDF2 — flagged as host-only 20.5ms, not an S3 number; NVS blob write time; assets mmap cost) as PENDING_HW rows where genuinely hardware-only, no fabricated numbers. **This completes the entire F track.** Docs-only, confirmed via git diff --stat. |
| F7.3 | done | d47b661 | msg_queue_reply() gained a `to` param (msg.h touched beyond Files list — F6.4 itself left a "that's F7.3" placeholder pointing at this exact change, flagged not guessed). scr_chat.c composer resolves leading @word against book.h's existing nickname/alias accessors (no new book.h entry point needed), strips it from body, omits `to` when it resolves to the default recipient, toasts "unknown @<word>" on miss (matching existing toast-then-stay pattern). This completes the F track except F8.1 (docs, last). **Real relay-routing verification, not just build:** ran address_book e2e scenario, confirmed via relay logs that a `to:"grandma"` /up is accepted (webhook 200), routed, and fanned out to the Twilio mock (201 Created) — the stack-driven equivalent of a serial transcript. make test + idf.py build green. |
| F7.2 | done | 12d4f42 | scr_pick.c (New message → pick recipient), scr_book.c (Address Book list + Add form + Nickname form), fixtures in render_png.c. **Reconciled a contradiction in the task text, documented not guessed:** the Add form spec says the phone field "accepts only + and digits" but its own mockup shows "(or leave blank and type an @alias)" directly under it, unreachable under a strict filter — implemented a leading `@` switching the field to alias charset, stripped before book_request(), matching book.h's own +=phone/else=alias contract. Worth a firmware-architect second look if that reading is wrong. **Flagged, not wired (scr_home.c out of Files list):** Home's "New message"/"Address book" rows still show F7.1-era stub toasts — a 2-line follow-up (point HROW_NEWMSG/HROW_BOOK at ui_push(&g_scr_pick)/&g_scr_book)) needed to make these screens reachable. Real bug found+fixed: -Werror=format-truncation false constraint from a shared struct's declared capacity vs. a smaller runtime cap — fixed buffer sizing. make png (12 PNGs) + idf.py build green. |
| F7.1 | done | 9f103d2 | book.c/h: full-replacement book_apply, nickname-slot preservation by alias, bv tracked and now reported for real in /status (replacing F3.6's hardcoded 0), book_request() builds/queues contact_req. modes.c dispatch order confirmed: lock→book→msg. No RTC struct for book (PROTOCOL.md §9.6 lists it NVS-only) — reuses g_rtc.auth for contact_req signing via new book_bind(), same cross-task pattern as msg_bind_auth(). Hand-verified book_parse() against the real authvectors.json book vector (byte-correct decode). idf.py build clean. Could not run the live address_book e2e at the time — the harness fix (f14c904) landed shortly after; the full suite (incl. address_book) now passes, indirectly confirming F7.1's book/cfg dispatch order is compatible with the real relay. |
| F6.5 | done | 45f1cb1 | lock.c/h (PBKDF2 via mbedtls_pkcs5_pbkdf2_hmac_ext — the non-deprecated successor; the task's named `_hmac` function doesn't exist in ESP-IDF 5.2's bundled mbedtls, confirmed by compiler error, mechanical fix not a design call), scr_lock.c, cfg/lock CBOR ingest (CFG_KEYMAP={"lock":0} cross-checked against wirecbor.py since PROTOCOL.md §10 only documents lock's children), deferred-shown while locked, restart-forces-locked, 5-attempts/30s-doubling-to-10min backoff. **Touched beyond Files list, flagged as a real unlock-bypass fix:** ui.c/h gained button-short/long gating on lock_is_locked() — without it a button press while Locked would open Chat/Home right over the passcode screen. Device-screen passcode-cycle UX (not-set→ask new; set→ask current then new, empty=off) is a documented minimal reading, no dedicated mockup existed. pager_rtc_t now 512B (672B under 1184B budget). make test (incl. PBKDF2 timing: 20.5ms/10000 iter) + idf.py build green; serial transcript built-unverified (no hardware). |
| F6.4 | done | b888f66 | msg.c/h rewritten: MSG_THREAD_DEPTH 32, per-peer `to`, msgq NVS namespace for full-fidelity reply/unread bodies (RTC now metadata-only), 320B/160-codepoint composer caps (UTF-8-atomic), msg_iter_peer(), cJSON fully removed (all devices, including req_sig==0, now CBOR-only RX — resolved DEVICE_TASKS.md's ambiguous wording in favor of full removal, consistent with F3.6/DEVICE_PLAN §9's cJSON-drop intent). README R6 accessor race fixed via lock-scoped copy into a static snapshot, keeping existing pointer-return signatures so scr_home.c/scr_chat.c/ui.c (out of scope) compile unchanged — they just don't yet use per-peer threading (expected to land in F7.x). Added cold-boot NVS recovery for pending replies/unread (not spelled out step-by-step but required for §9.2's "survives a battery pull" claim). sizeof(pager_rtc_t)=496 (~480 target). make test + idf.py build green. |
| F6.3 | done | 817714a | ui.c/h rewritten: screen stack (depth≤4), status bar (bucket redraw-on-change via disp.c's existing partial-refresh diff), scr_home/chat/device/setup.c new. Render moved to modes_run()'s main task via render_pending handoff, shown only marked after a render actually displayed (README R5/R7 closed). Incoming-message steal-the-screen policy matches §5.5. **Correctly deferred, documented:** address-book/passcode-lock UI (needs book.c/lock.c — F7.1/F6.5, don't exist yet); "Set up again" from Device screen (setup_run() only safe pre-modes_boot() per setup.c's own no-overlap assumption — calling it post-boot needs setup.c/net.cpp changes outside this task); scr_setup.c is real but currently unreachable (main.c's ident-missing path predates modes_boot()); signal bar's "not registered" bucket collapses into "0 bars" (net_get_rssi() exposes no registration-state bit). make png (8 PNGs) + make test + idf.py build all green. Serial transcript correctly not fabricated — no hardware/QEMU harness attached. |
| F6.2 | done | 3e69d52 | input.c/h (key decode table incl. 0xB4-0xB7/0x1B/0x09, button FSM w/ BTN_STUCK moved from modes.c, one event queue, 30s input_awake() window), ime.h (identity IME, header-only, not wired to a text field until F6.3). input_feed_key() is the entry point since ui.c keeps sole ownership of the CardKB I2C read until F6.3 (two readers on one buffered peripheral would race — correctly not merged). **Flagged, not silently patched:** BTN_STUCK only coarsens modes_run()'s poll cadence — it does NOT resume net_sleep() early, since net_sleep() arms level-0 (active-low) ext0 wakeup and a held button would otherwise spin light-sleep re-entry against RTS toggling on every call; a real fix needs a level-1 wake-on-release net_sleep() variant in net.cpp, out of this task's Files list. host test (decoder table, exhaustive 0x00-0xFF) + idf.py build both green. |
| F6.1 | done | 9ee35fe | disp.c (SSD1680 driver, moved), gfx.c (framebuffer/UTF-8/glyph blit/text_wrap), mkassets.py (real Noto Sans + Noto Sans CJK SC found on this machine — no substitute needed), render_png.c (host PNG renderer, hand-written PNG writer over zlib). Retry built on a prior rate-limit-killed attempt's partial work after inspecting it and judging it sound. **Real bug found+fixed:** mkassets.py packs 10-byte glyph records but gfx.c's decoder assumed 9 bytes (gfx.h's own comment was self-inconsistent) — corrupted every glyph past the first, segfaulted in blit_glyph(); fixed. **Documented limitation, not silently patched:** CJK glyph selection uses code-point order as a frequency stand-in (no offline GB2312 frequency table available), so a few common hanzi render as tofu — flagged in mkassets.py's own docstring, not a new bug. assets.bin 583 KiB (<1MB), make test/png/idf.py build all green. **Note:** DEVICE_TASKS.md's literal Verify command omits required --sans-font/--cjk-font flags (mkassets.py never fabricates a default path) — ran with explicit real font paths instead. PNGs/assets.bin left as build/ artifacts (untracked, matching existing repo convention) — plan's Definition-of-done wants PNGs "committed as artefacts in the PR"; revisit when a PR is opened. |
| S2b.5 | done, one mode skipped (see Blocked) | 7426960 | e2e scenario setup_code: real admin-create→code→bootstrap_device()→provisioned flow, boot-{bid} EMQX user cleanup confirmed both on completion and on expiry (setupCodes.expiresAt patched, devsetup.expire() called directly, retained message + credential confirmed gone via EMQX's HTTP API, retry against expired code fails for real). ALL PASSED. This completes the S2/S2b/T2b track. **Not implemented, correctly not guessed:** the `BROKER_MANAGES_AUTH=0` shared-boot-user variant — DEVICE_PLAN.md §3.2 itself marks this "NEEDS HUMAN DECISION" and has no ACL-scoping design for a wildcard-topic shared credential; verified nothing in devsetup.py/emqx_admin.py/pager_client.py implements it (not a missed wiring, a genuine unresolved design gap). See Blocked section. **TODO(orchestrator), minor:** pager_client.bootstrap_device()'s _on_connect doesn't check reason_code, so it can print a misleading "connected" line on a broker-rejected CONNACK before the real timeout/RuntimeError fires correctly. |
| T2b.4 | done | 78c7d1c | pager_client.py --bootstrap CODE: parse/derive via relay's own devsetup.py, connects as boot-{bid}, decrypts retained bundle, acks, reconnects as the real signed device. **Fully verified live** against docker compose: real admin-issued setup code → bootstrap → provisionState flips to "provisioned" on first signed /status → boot-{bid} EMQX user gone afterward (S2b.1's complete() revoked it). 477/477 regression suite. --port override added since admin API has no way to configure a non-8883 bootstrap port yet (bundle always says 8883; compose stack is plain 1883) — flagged as a gap, not fixed (out of Files scope). |
| W4.4 | done | fcaab8c | contacts/page.tsx (pending list, approve/reject dialogs), devices page gained Lock column (auto-lock select, clear-passcode button) → POST /cfg. **Worked around, not guessed:** phoneIndex is server-only/unreadable by the client, so the "link suggestion" is reconstructed client-side from readable backends subcollections matching the same condition the server uses to write phoneIndex; server re-validates on approve regardless, so a stale client guess can't misfile a contact. **Flagged, minor:** AppShell.tsx's nav has no /admin/contacts entry — page only reachable by direct URL, one-line follow-up. build/tsc/lint all clean. |
| S2b.3 | done | bda8eab | tools/emqx_setup.py routes pager/boot/+/up through the existing webhook rule. webhooks.py dispatches boot-topic acks to new ingest.py `handle_boot_ack` (decodes {v:1,ok:1}, calls devsetup.complete — no signature check needed, ACL on the boot-{bid} credential is the auth boundary for this namespace). provisionState→"provisioned" wired into handle_status's already-signature-verified path for hmac devices. 10/10 + 477/477 full suite. **Operational note:** this agent (and T3.7 before it) hit a git-index race from concurrent agents committing in the same working tree — both self-corrected without data loss, but running many parallel agents against one shared working tree/index has real (so-far-recoverable) risk; worth keeping parallelism moderate. |
| W2.3 | done | 89a852b | Devices page rewired to S2.2's setup-code response shape; new SetupCodePanel.tsx (QR via qrcode npm pkg, copy button, expiresAt countdown, two-step §3.2 instructions, online/waiting banner, manualAcl lines when brokerPush=="manual"); revoke now calls the real mounted route; "Provisioned" column added. build/tsc/lint all clean. README checklist entry added (touched beyond Files list per Do step, flagged, established precedent). |
| T3.7 | done | e2df389 | provision.py: --port/--code serial flow (streams to SETUP done|error, exit code reflects result); --from-api calls admin device-create then feeds the returned setupCode into the same flow. Added required --device-id flag (CreateDeviceRequest requires it; task's flag list omitted it — not a design choice, just a schema requirement). Verified against a pty-based fake serial console (success + error paths), --help, and argument-validation exit codes. **TODO(orchestrator):** pyserial/httpx not declared as project dependencies anywhere reachable by this file (lazy-imported with a clear error instead) — needs a home in relay/pyproject.toml or a tools/ requirements file. Self-corrected a git-index race with a concurrent agent (accidental `git add -A`-equivalent swept in their staged changes; caught immediately, `git reset --mixed`, recommitted with an explicit pathspec — final commit is one file, nothing lost). |
| F3.6 | done | 21899a5 | msg.c/modes.c: CBOR publish path (msg_ingest_down_cbor, publish_ack/reply sign via auth_sign when IDENT_FLAG_REQ_SIG), /status adds rssi/bv/n/sig, RX branches signed(CBOR+auth_verify)/unsigned(JSON, until F6.4 removes cJSON), pager_rtc_t embeds auth_rtc_t, PAGER_RTC_MAGIC 2→3, epoch-wrap NVS persist. **Worth a human/architect look:** TX is unconditionally CBOR for all devices (DEVICE_PLAN.md §2.4 reading) but n/sig only appended when req_sig set (PROTOCOL.md §14 reading) — i.e. an unsigned device now sends unsigned CBOR, not JSON, on the wire. Flagged in case the intent was JSON-until-signed. make test + idf.py build green; relay-integration serial transcript built-unverified (no hardware). |
| S4.5 | done (confirmed via later full-suite run) | 387a37e | pager_client.py handles book/cfg apply+ack, sends contact_req; e2e_v2.py scenario_address_book written per spec. Originally blocked at device-creation by the broker.py binary-payload bug, then by the e2e-harness staleness — both since fixed (40532fe, f14c904); `address_book` now passes for real in both wire modes as part of the full 11-scenario suite. |
| — | reordered | — | Ran **S2b.1 before S2.2**, contrary to DEVICE_TASKS.md's dependency-graph text: S2.2's own Do steps call S2b.1's `devsetup.issue()`, a real forward dependency the graph diagram doesn't show. |
| S4.2 | done | 1fa7e68 | devcfg.py: build_book/push_book/push_cfg/ack/republish_pending. admin.py cfg endpoint + approve/reject repointed to devcfg.push_book (contacts.py's S4.1 stub is now dead code — minor cleanup, not urgent). In-flight book/cfg state stored as raw `devices/{d}.pendingBook`/`.pendingCfg` fields (no dedicated collection in SERVER_PLAN.md, same undeclared-field precedent as S4.1). **Flagged, not resolved:** PROTOCOL.md §3.1 marks `d` required on `book`, but defaultToUid is optional — `d` omitted when unset, mirroring existing "omit `to`" precedent. 26/26 new tests, 445/445 full suite. |
| S2b.2 | done | b07e0c0 | ca_resolve.py: BROKER_CA_PEM config wins, else fetches served chain from broker_host:8883, matches root against certifi by issuer subject, caches. config.py gained broker_host/broker_ca_pem settings. 9/9 new tests, 457/457 full suite. **TODO(orchestrator), 2 one-liners once admin.py is free:** (1) GET /api/admin/settings needs `brokerCaSubject: ca_resolve.get_broker_ca_subject()`; (2) admin.py's `_bootstrap_host_and_ca()` should read `Settings.broker_host`/`.broker_ca_pem` instead of raw env vars (S2.2's stopgap). Also: app/main.py's lifespan() should call `resolve_broker_ca(settings)` once at startup (currently only resolves on first explicit call); BROKER_HOST/BROKER_CA_PEM not yet added to .env.example; certifi not yet an explicit pyproject.toml dependency (transitive only). |
| S2.2 | done | 1d53553 | POST /api/admin/devices + rotate-credentials share DeviceSetupCodeResponse shape; revoke mounted; mqttPasswordHash removed from devices/{d} (moved to deviceSecrets, per S1.1's flagged migration). Wired the two loose ends S1.3 left uncalled: clear_auth_alarm and invalidate_secret_cache now fire on rotate. BROKER_HOST/BROKER_CA_PEM read directly from env as a stopgap for S2b.2's real config — documented for S2b.2 to replace. 17/17 + 210/210 regression tests pass (a full-suite run hit shared-docker-compose contention from a concurrent agent tearing down the stack — confirmed environmental, not a regression, after isolated re-runs). |
| S2b.1 | done | bf67a6f | devsetup.py (token/parse/derive/bundle/issue/complete/expire), setup_codes.py store. **For S2.2's agent:** `issue()` takes plaintext `mqtt_password`/`hmac_key`/`host`/`ca`/`label`/`port`/`flags`/`apn` as explicit kwargs (relay never stores plaintext password; per-deployment host/CA config fields don't exist yet) — S2.2 must generate these values and pass them in, not look up a device. **Deviation flagged:** vector went into new `tools/setup_code_vectors.json`, not `tools/authvectors.json` (appending there would break test_devauth.py's exact-8-vector/shape assertions, out of this task's scope to fix). `broker.publish_raw` wasn't created — existing `broker.publish(topic, payload, qos, retain)` already supported retain. **TODO(orchestrator):** `devsetup.expire()` implemented but not wired into `app/jobs.py`'s `tick()` (jobs.py out of scope) — needs a follow-up task/call site. |
